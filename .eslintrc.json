{
  "extends": "next/core-web-vitals",
  "rules": {
    "@typescript-eslint/no-unused-vars": [
      "error",
      {
        "varsIgnorePattern": "^(dir|children)$",
        "argsIgnorePattern": "^(dir|children)$"
      }
    ]
  }
}
